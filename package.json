{
  "name": "autochess",
  "version": "1.0.0",
  "description": "Auto Chess. Literally.",
  "author": "Jasper Stephenson",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "golive": "npm run generate && cp -rf ./dist/* ../personalsite2018/docs/autochess && cp -rf ./dist/* ../personalsite2018/static/autochess && cd ../personalsite2018 && git add ./docs/autochess && git commit -m \"Updated Autochess\" && git push && cd ../autochess"
  },
  "dependencies": {
    "firebase": "^6.4.2",
    "node-sass": "^4.13.1",
    "nuxt": "^2.0.0",
    "sass-loader": "^7.3.1"
  },
  "devDependencies": {
    "dotenv": "^8.1.0"
  }
}
